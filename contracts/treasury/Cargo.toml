[package]
name = "treasury"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]
doctest = false

[dependencies]
soroban-sdk = { workspace = true }
stream = { path = "../stream" }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
